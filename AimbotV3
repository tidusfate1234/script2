local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/SiriusSoftwareLtd/Rayfield/main/source.lua'))()

local Window = Rayfield:CreateWindow({
    Name = "Arsenal Aimbot V5 Insane",
    LoadingTitle = "Arsenal Aimbot",
    LoadingSubtitle = "by West",
    ConfigurationSaving = { Enabled = false }
})

local Tab = Window:CreateTab("Main", 4483362458)
local TabSettings = Window:CreateTab("Settings", 4483362458)
local TabPriority = Window:CreateTab("Priority", 4483362458)

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")

-- Variables
local Camera = Workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

-- Settings
local Settings = {
    Enabled = false,
    SilentAim = false,
    TeamCheck = true,
    AimPart = "Head",
    FOV = 120,
    Smoothness = 1.0, -- Changed default for lock-on
    Prediction = 0.13,
    ShowFOV = true,
    FOVColor = Color3.fromRGB(255, 0, 0), -- Changed to red for lock-on
    TriggerBot = false,
    AutoShoot = false,
    TriggerDelay = 0.05,
    VisibilityCheck = true,
    -- Lock-On Settings
    LockOnMode = true, -- NEW: Full lock-on toggle
    StickyAim = true, -- NEW: Keep locked until target dies/leaves FOV
    SwitchTargetKey = "Q", -- NEW: Key to switch targets
    -- Premium Settings
    SmartTargeting = true,
    DynamicPrediction = true,
    Humanization = true,
    TargetLockTime = 999, -- Increased for lock-on
    CrosshairWeight = 1.2,
    DistanceWeight = 0.02,
    VelocityWeight = 0.4,
    HealthWeight = 0.1,
    JitterAmount = 0.0, -- Disabled for lock-on
    SmoothVariation = 0.0, -- Disabled for lock-on
    -- V5 Insane Settings
    AdaptiveFOV = false, -- Disabled for lock-on
    HitChance = 100,
    ParallelScanning = true,
    ScanInterval = 0.05, -- Faster for lock-on
    PrioritySystem = "Crosshair",
    AdaptiveSmoothness = false, -- Disabled for lock-on
    RecoilControl = false
}

-- FOV Circle
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.NumSides = 64
FOVCircle.Filled = false
FOVCircle.Color = Settings.FOVColor
FOVCircle.Transparency = 0.85
FOVCircle.Visible = false

-- Lock-On Indicator
local LockIndicator = Drawing.new("Circle")
LockIndicator.Thickness = 3
LockIndicator.NumSides = 32
LockIndicator.Filled = false
LockIndicator.Color = Color3.fromRGB(255, 0, 0)
LockIndicator.Transparency = 1
LockIndicator.Visible = false
LockIndicator.Radius = 30

-- Global RaycastParams
local RaycastParams = RaycastParams.new()
RaycastParams.FilterType = Enum.RaycastFilterType.Blacklist
RaycastParams.IgnoreWater = true

-- Target Lock System
local TargetLock = {
    Player = nil,
    Part = nil,
    ExpireTime = 0,
    LockStartTime = 0,
    IsLocked = false -- NEW: Lock state
}

-- Damage Tracker
local DamageTracker = {
    LastDamager = nil,
    DamageTime = 0
}

-- Parallel Scanner Cache
local ScanCache = {
    BestTarget = nil,
    BestPlayer = nil,
    AllTargets = {} -- NEW: Store all valid targets
}

-- Timing variables
local LastScanTime = 0
local LastTriggerTime = 0
local LastTargetRefresh = 0
local TargetRefreshDelay = 0.01 -- Faster for lock-on
local LastSwitchTime = 0
local SwitchCooldown = 0.2

local RenderConnection
local HeartbeatConnection

-- Silent Aim connections table
local SilentAimConnections = {}
local OldNamecall = nil

-- Safe Character Getter
local function GetCharacter(Player)
    local Char = Player.Character
    if not Char then return nil end
    
    local Humanoid = Char:FindFirstChildOfClass("Humanoid")
    local Root = Char:FindFirstChild("HumanoidRootPart")
    
    if Humanoid and Humanoid.Health > 0 and Root then
        return Char
    end
    
    return nil
end

-- Helper Functions
local function HumanizePosition(Position)
    if not Settings.Humanization then return Position end
    
    local Jitter = Vector3.new(
        (math.random() - 0.5) * Settings.JitterAmount * 2,
        (math.random() - 0.5) * Settings.JitterAmount * 2,
        (math.random() - 0.5) * Settings.JitterAmount * 2
    )
    return Position + Jitter
end

local function GetDynamicPrediction(Part)
    if not Settings.DynamicPrediction then return Settings.Prediction end
    
    local Velocity = Part.AssemblyLinearVelocity.Magnitude
    
    if Velocity < 5 then
        return 0.09
    elseif Velocity < 20 then
        return 0.12
    else
        return 0.16
    end
end

local function ShouldHit()
    if Settings.HitChance >= 100 then return true end
    return math.random(1, 100) <= Settings.HitChance
end

local function ScoreTarget(Player, CrosshairDistance, Distance, Velocity, Health)
    local Score = 
        (CrosshairDistance * Settings.CrosshairWeight) +
        (Distance * Settings.DistanceWeight) +
        (Velocity * Settings.VelocityWeight) +
        ((100 - Health) * Settings.HealthWeight)
    
    if Settings.PrioritySystem == "Health" then
        Score = Score * (1 - (Health / 200))
    elseif Settings.PrioritySystem == "Distance" then
        Score = Score * (1 / (Distance + 1))
    elseif Settings.PrioritySystem == "Damage" and DamageTracker.LastDamager == Player then
        if tick() - DamageTracker.DamageTime < 3 then
            Score = Score * 0.5
        end
    end
    
    return Score
end

local function IsTeammate(Player)
    if not Settings.TeamCheck then return false end
    if not Player.Team or not LocalPlayer.Team then return false end
    return Player.Team == LocalPlayer.Team
end

local function IsVisible(Part)
    if not Settings.VisibilityCheck then return true end
    if not Part then return false end
    
    local Origin = Camera.CFrame.Position
    local Direction = (Part.Position - Origin)
    local Distance = Direction.Magnitude
    
    if Distance > 500 then return false end
    
    local Char = LocalPlayer.Character
    if Char then
        RaycastParams.FilterDescendantsInstances = {Char}
    end
    
    local Result = Workspace:Raycast(Origin, Direction.Unit * Distance, RaycastParams)
    
    if not Result then return true end
    
    local HitInstance = Result.Instance
    local HitModel = HitInstance:FindFirstAncestorWhichIsA("Model")
    
    return HitModel == Part.Parent or HitInstance:IsDescendantOf(Part.Parent)
end

local function GetPredictedPosition(Part)
    if not Part then return nil end
    
    local Velocity = Part.AssemblyLinearVelocity or Vector3.zero
    local PredictionAmount = GetDynamicPrediction(Part)
    local PredictedPos = Part.Position + (Velocity * PredictionAmount)
    
    return HumanizePosition(PredictedPos)
end

-- Switch to next valid target
local function SwitchToNextTarget()
    if not Settings.LockOnMode then return end
    if #ScanCache.AllTargets == 0 then return end
    
    local CurrentTime = tick()
    if CurrentTime - LastSwitchTime < SwitchCooldown then return end
    LastSwitchTime = CurrentTime
    
    -- Find current target index
    local CurrentIndex = 0
    if TargetLock.Player then
        for i, data in ipairs(ScanCache.AllTargets) do
            if data.Player == TargetLock.Player then
                CurrentIndex = i
                break
            end
        end
    end
    
    -- Get next target
    local NextIndex = (CurrentIndex % #ScanCache.AllTargets) + 1
    local NextTarget = ScanCache.AllTargets[NextIndex]
    
    if NextTarget then
        TargetLock.Player = NextTarget.Player
        TargetLock.Part = NextTarget.Part
        TargetLock.ExpireTime = tick() + Settings.TargetLockTime
        TargetLock.LockStartTime = tick()
        TargetLock.IsLocked = true
    end
end

-- Target Scanning
local function ScanTargets()
    local CurrentTime = tick()
    
    if not Settings.ParallelScanning then return end
    if CurrentTime - LastScanTime < Settings.ScanInterval then return end
    
    LastScanTime = CurrentTime
    local BestPart = nil
    local BestPlayer = nil
    local BestScore = math.huge
    
    local CameraPos = Camera.CFrame.Position
    local ViewportSize = Camera.ViewportSize
    local ScreenCenter = Vector2.new(ViewportSize.X / 2, ViewportSize.Y / 2)
    
    local CurrentFOV = Settings.FOV
    
    -- Clear all targets list
    ScanCache.AllTargets = {}
    
    for _, Player in ipairs(Players:GetPlayers()) do
        if Player ~= LocalPlayer then
            local Character = GetCharacter(Player)
            if Character and not IsTeammate(Player) then
                local AimPart = Character:FindFirstChild(Settings.AimPart) or Character:FindFirstChild("HumanoidRootPart")
                
                if AimPart and IsVisible(AimPart) then
                    local ScreenPos, OnScreen = Camera:WorldToViewportPoint(AimPart.Position)
                    
                    if OnScreen then
                        local CrosshairDistance = (Vector2.new(ScreenPos.X, ScreenPos.Y) - ScreenCenter).Magnitude
                        
                        if CrosshairDistance < CurrentFOV then
                            -- Add to all targets list
                            table.insert(ScanCache.AllTargets, {
                                Player = Player,
                                Part = AimPart,
                                Distance = (CameraPos - AimPart.Position).Magnitude,
                                CrosshairDistance = CrosshairDistance
                            })
                            
                            if Settings.SmartTargeting then
                                local Distance = (CameraPos - AimPart.Position).Magnitude
                                local Velocity = AimPart.AssemblyLinearVelocity.Magnitude
                                local Health = 100
                                local Humanoid = Character:FindFirstChildOfClass("Humanoid")
                                if Humanoid then
                                    Health = Humanoid.Health
                                end
                                
                                local Score = ScoreTarget(Player, CrosshairDistance, Distance, Velocity, Health)
                                if Score < BestScore then
                                    BestScore = Score
                                    BestPart = AimPart
                                    BestPlayer = Player
                                end
                            else
                                if CrosshairDistance < BestScore then
                                    BestScore = CrosshairDistance
                                    BestPart = AimPart
                                    BestPlayer = Player
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    
    -- Sort all targets by crosshair distance
    table.sort(ScanCache.AllTargets, function(a, b)
        return a.CrosshairDistance < b.CrosshairDistance
    end)
    
    -- Handle lock-on logic
    if Settings.LockOnMode then
        if not TargetLock.IsLocked or not Settings.StickyAim then
            -- Initial lock or non-sticky mode
            if BestPart and BestPlayer then
                TargetLock.Player = BestPlayer
                TargetLock.Part = BestPart
                TargetLock.ExpireTime = tick() + Settings.TargetLockTime
                TargetLock.LockStartTime = tick()
                TargetLock.IsLocked = true
            end
        else
            -- Sticky aim: check if current target is still valid
            if TargetLock.Player then
                local LockChar = GetCharacter(TargetLock.Player)
                if LockChar and not IsTeammate(TargetLock.Player) then
                    local LockPart = LockChar:FindFirstChild(Settings.AimPart) or LockChar:FindFirstChild("HumanoidRootPart")
                    if LockPart and IsVisible(LockPart) then
                        local ScreenPos, OnScreen = Camera:WorldToViewportPoint(LockPart.Position)
                        if OnScreen then
                            local ScreenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
                            local Distance = (Vector2.new(ScreenPos.X, ScreenPos.Y) - ScreenCenter).Magnitude
                            if Distance < Settings.FOV then
                                -- Keep current target
                                BestPart = LockPart
                                BestPlayer = TargetLock.Player
                                TargetLock.Part = LockPart
                                TargetLock.ExpireTime = tick() + Settings.TargetLockTime
                            end
                        end
                    end
                end
            end
        end
    end
    
    ScanCache.BestTarget = BestPart
    ScanCache.BestPlayer = BestPlayer
end

local function GetBestTarget()
    local CurrentTime = tick()
    
    -- Check target lock first
    if TargetLock.Player and TargetLock.IsLocked then
        if Settings.StickyAim or CurrentTime < TargetLock.ExpireTime then
            local LockChar = GetCharacter(TargetLock.Player)
            if LockChar and not IsTeammate(TargetLock.Player) then
                local LockPart = LockChar:FindFirstChild(Settings.AimPart) or LockChar:FindFirstChild("HumanoidRootPart")
                if LockPart and IsVisible(LockPart) then
                    local ScreenPos, OnScreen = Camera:WorldToViewportPoint(LockPart.Position)
                    if OnScreen then
                        local ScreenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
                        local Distance = (Vector2.new(ScreenPos.X, ScreenPos.Y) - ScreenCenter).Magnitude
                        if Distance < Settings.FOV then
                            return LockPart, TargetLock.Player
                        end
                    end
                end
            end
        end
        -- Target lost or expired
        TargetLock.Player = nil
        TargetLock.Part = nil
        TargetLock.IsLocked = false
    end
    
    -- Return cached target if within refresh delay
    if CurrentTime - LastTargetRefresh < TargetRefreshDelay then
        return ScanCache.BestTarget, ScanCache.BestPlayer
    end
    
    -- Refresh target
    LastTargetRefresh = CurrentTime
    ScanTargets()
    return ScanCache.BestTarget, ScanCache.BestPlayer
end

-- Trigger Bot with cooldown
local function TriggerBotCheck()
    if not (Settings.TriggerBot and Settings.Enabled) then return end
    if not ShouldHit() then return end
    
    local CurrentTime = tick()
    if CurrentTime - LastTriggerTime < Settings.TriggerDelay then return end
    
    local Character = GetCharacter(LocalPlayer)
    if not Character then return end
    
    local Target, _ = GetBestTarget()
    if not Target then return end
    
    if Settings.AutoShoot then
        LastTriggerTime = CurrentTime
        local MousePos = UserInputService:GetMouseLocation()
        VirtualInputManager:SendMouseButtonEvent(MousePos.X, MousePos.Y, 0, true, game, 1)
        task.wait(0.016)
        VirtualInputManager:SendMouseButtonEvent(MousePos.X, MousePos.Y, 0, false, game, 1)
    end
end

-- Silent Aim Hook
local function SetupSilentAim()
    for _, conn in ipairs(SilentAimConnections) do
        pcall(function() conn:Disconnect() end)
    end
    SilentAimConnections = {}
    
    if OldNamecall then
        pcall(function() OldNamecall = nil end)
    end
    
    local Success, Err = pcall(function()
        OldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(Self, ...)
            local Method = getnamecallmethod()
            local Args = {...}
            
            if Method == "FireServer" and Settings.Enabled and Settings.SilentAim and ShouldHit() then
                if Self:IsA("RemoteEvent") then
                    local Parent = Self.Parent
                    if Parent == ReplicatedStorage or (Parent and Parent:FindFirstAncestorWhichIsA("Tool")) then
                        local Target, _ = GetBestTarget()
                        if Target then
                            local PredictedPos = GetPredictedPosition(Target)
                            
                            for i, Arg in pairs(Args) do
                                if typeof(Arg) == "Vector3" then
                                    Args[i] = PredictedPos
                                    break
                                elseif typeof(Arg) == "CFrame" then
                                    Args[i] = CFrame.new(PredictedPos)
                                    break
                                elseif typeof(Arg) == "table" then
                                    if Arg.Position and typeof(Arg.Position) == "Vector3" then
                                        Arg.Position = PredictedPos
                                        break
                                    elseif Arg.hit and Arg.hit.Position and typeof(Arg.hit.Position) == "Vector3" then
                                        Arg.hit.Position = PredictedPos
                                        break
                                    end
                                end
                            end
                        end
                    end
                end
            end
            
            return OldNamecall(Self, unpack(Args))
        end))
    end)
    
    if Success then
        print("✅ Silent Aim: Hooked successfully")
    else
        warn("❌ Silent Aim hook failed:", Err)
    end
end

-- Key Input Handler for target switching
UserInputService.InputBegan:Connect(function(Input, GameProcessed)
    if GameProcessed then return end
    if not Settings.Enabled then return end
    if not Settings.LockOnMode then return end
    
    if Input.KeyCode == Enum.KeyCode[Settings.SwitchTargetKey] then
        SwitchToNextTarget()
    end
end)

-- Initialize Silent Aim
SetupSilentAim()

-- Parallel Scanner
HeartbeatConnection = RunService.Heartbeat:Connect(function()
    if Settings.Enabled and Settings.ParallelScanning then
        ScanTargets()
    end
end)

-- Main Loop - Full Lock-On
RenderConnection = RunService.RenderStepped:Connect(function()
    if not Settings.Enabled then
        FOVCircle.Visible = false
        LockIndicator.Visible = false
        return
    end
    
    -- Get current target
    local CurrentTarget, TargetPlayer = GetBestTarget()
    
    local CurrentFOV = Settings.FOV
    local ViewportSize = Camera.ViewportSize
    
    -- Update FOV Circle
    FOVCircle.Position = Vector2.new(ViewportSize.X / 2, ViewportSize.Y / 2)
    FOVCircle.Radius = CurrentFOV
    FOVCircle.Visible = Settings.ShowFOV
    FOVCircle.Color = Settings.FOVColor
    
    -- Update Lock Indicator
    if TargetLock.IsLocked and CurrentTarget then
        local ScreenPos, OnScreen = Camera:WorldToViewportPoint(CurrentTarget.Position)
        if OnScreen then
            LockIndicator.Position = Vector2.new(ScreenPos.X, ScreenPos.Y)
            LockIndicator.Visible = true
            LockIndicator.Color = Color3.fromRGB(255, 0, 0)
        else
            LockIndicator.Visible = false
        end
    else
        LockIndicator.Visible = false
    end

    -- FULL LOCK-ON Aimbot (No smoothing)
    if not Settings.SilentAim then
        local ShouldAim = false
        
        if Settings.LockOnMode then
            -- Always aim when locked on
            ShouldAim = TargetLock.IsLocked and CurrentTarget ~= nil
        else
            -- Traditional right-click aim
            ShouldAim = UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2)
        end
        
        if ShouldAim and CurrentTarget then
            if ShouldHit() then
                local TargetPos = GetPredictedPosition(CurrentTarget)
                -- DIRECT LOCK - No lerp, instant snap
                Camera.CFrame = CFrame.new(Camera.CFrame.Position, TargetPos)
            end
        end
    end

    -- Trigger Bot
    TriggerBotCheck()
end)

-- UI Elements - Main Tab
Tab:CreateToggle({ Name = "Aimbot Enabled", CurrentValue = false, Callback = function(v) Settings.Enabled = v end })
Tab:CreateToggle({ Name = "Lock-On Mode", CurrentValue = true, Callback = function(v) Settings.LockOnMode = v end })
Tab:CreateToggle({ Name = "Sticky Aim", CurrentValue = true, Callback = function(v) Settings.StickyAim = v end })
Tab:CreateToggle({ Name = "Silent Aim", CurrentValue = false, Callback = function(v) Settings.SilentAim = v end })
Tab:CreateToggle({ Name = "Trigger Bot", CurrentValue = false, Callback = function(v) Settings.TriggerBot = v end })
Tab:CreateToggle({ Name = "Auto Shoot", CurrentValue = false, Callback = function(v) Settings.AutoShoot = v end })
Tab:CreateToggle({ Name = "Team Check", CurrentValue = true, Callback = function(v) Settings.TeamCheck = v end })
Tab:CreateToggle({ Name = "Visibility Check", CurrentValue = true, Callback = function(v) Settings.VisibilityCheck = v end })
Tab:CreateToggle({ Name = "Show FOV Circle", CurrentValue = true, Callback = function(v) Settings.ShowFOV = v end })

Tab:CreateSlider({ Name = "FOV Size", Range = {50, 500}, Increment = 5, CurrentValue = Settings.FOV, Callback = function(v) Settings.FOV = v end })
Tab:CreateSlider({ Name = "Hit Chance", Range = {0, 100}, Increment = 1, CurrentValue = Settings.HitChance, Callback = function(v) Settings.HitChance = v end })
Tab:CreateSlider({ Name = "Trigger Delay", Range = {0.01, 0.2}, Increment = 0.01, CurrentValue = Settings.TriggerDelay, Callback = function(v) Settings.TriggerDelay = v end })

Tab:CreateDropdown({ Name = "Aim Part", Options = {"Head", "HumanoidRootPart", "UpperTorso", "LowerTorso"}, CurrentOption = {Settings.AimPart}, Callback = function(o) Settings.AimPart = o[1] end })
Tab:CreateDropdown({ Name = "Switch Target Key", Options = {"Q", "E", "R", "F", "C", "X", "Z"}, CurrentOption = {Settings.SwitchTargetKey}, Callback = function(o) Settings.SwitchTargetKey = o[1] end })

Tab:CreateColorPicker({ Name = "FOV Circle Color", Color = Settings.FOVColor, Callback = function(c) Settings.FOVColor = c; FOVCircle.Color = c end })

-- Premium Settings Tab
TabSettings:CreateToggle({ Name = "Smart Targeting", CurrentValue = true, Callback = function(v) Settings.SmartTargeting = v end })
TabSettings:CreateToggle({ Name = "Dynamic Prediction", CurrentValue = true, Callback = function(v) Settings.DynamicPrediction = v end })
TabSettings:CreateToggle({ Name = "Humanization", CurrentValue = false, Callback = function(v) Settings.Humanization = v end })
TabSettings:CreateToggle({ Name = "Parallel Scanning", CurrentValue = true, Callback = function(v) Settings.ParallelScanning = v end })

TabSettings:CreateSlider({ Name = "Scan Interval", Range = {0.01, 0.5}, Increment = 0.01, CurrentValue = Settings.ScanInterval, Callback = function(v) Settings.ScanInterval = v end })
TabSettings:CreateSlider({ Name = "Jitter Amount", Range = {0, 0.1}, Increment = 0.005, CurrentValue = Settings.JitterAmount, Callback = function(v) Settings.JitterAmount = v end })

-- Priority Tab
TabPriority:CreateDropdown({ 
    Name = "Priority System", 
    Options = {"Crosshair", "Health", "Distance", "Damage"}, 
    CurrentOption = {Settings.PrioritySystem}, 
    Callback = function(o) Settings.PrioritySystem = o[1] end 
})

TabPriority:CreateSlider({ Name = "Crosshair Weight", Range = {0, 5}, Increment = 0.1, CurrentValue = Settings.CrosshairWeight, Callback = function(v) Settings.CrosshairWeight = v end })
TabPriority:CreateSlider({ Name = "Distance Weight", Range = {0, 1}, Increment = 0.01, CurrentValue = Settings.DistanceWeight, Callback = function(v) Settings.DistanceWeight = v end })
TabPriority:CreateSlider({ Name = "Velocity Weight", Range = {0, 1}, Increment = 0.01, CurrentValue = Settings.VelocityWeight, Callback = function(v) Settings.VelocityWeight = v end })
TabPriority:CreateSlider({ Name = "Health Weight", Range = {0, 1}, Increment = 0.01, CurrentValue = Settings.HealthWeight, Callback = function(v) Settings.HealthWeight = v end })

-- Info Label
Tab:CreateLabel({ Name = "Lock-On Info", Text = "🎯 Full Lock-On Active\nPress " .. Settings.SwitchTargetKey .. " to switch targets" })

-- Unload Button
Tab:CreateButton({
    Name = "Unload Script",
    Callback = function()
        Settings.Enabled = false
        Settings.SilentAim = false
        
        if RenderConnection then RenderConnection:Disconnect() end
        if HeartbeatConnection then HeartbeatConnection:Disconnect() end
        for _, conn in ipairs(SilentAimConnections) do
            pcall(function() conn:Disconnect() end)
        end
        
        FOVCircle:Remove()
        LockIndicator:Remove()
        Rayfield:Destroy()
    end
})

-- Camera & Respawn Handling
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    ScanCache.BestTarget = nil
    ScanCache.BestPlayer = nil
    ScanCache.AllTargets = {}
    TargetLock.Player = nil
    TargetLock.Part = nil
    TargetLock.IsLocked = false
    LastTargetRefresh = 0
end)

Workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
    Camera = Workspace.CurrentCamera
end)
